import React from 'react';

export default function App(porps) {
    return (
        <div>
            <h1>React Email Router</h1>
                <div>
                    {props.children}
                </div>
        </div>
    );
};